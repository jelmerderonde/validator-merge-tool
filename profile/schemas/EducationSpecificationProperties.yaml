required:
  - primaryCode
  - educationSpecificationType
  - name
  - validFrom
  - formalDocument
  - level
  - sector
properties:
  consumers:
    # in addition to general Consumer `items` (in base), there should
    # be at least one RioConsumer
    contains:
      $ref: './EducationSpecificationRioConsumer.yaml'
      
