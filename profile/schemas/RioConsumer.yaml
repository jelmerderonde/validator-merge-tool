allOf:
  - $ref: './Consumer.yaml'
  - properties:
      consumerKey:
        const: rio
      formalDocument:
        type: string
        enum:
          - diploma
          - certificate
          - no official document
          - school advice
          - testimonial
